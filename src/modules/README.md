# Vault-OS modules

This directory will contain optional, release-managed capabilities that are not
required by every vault. Candidate modules include PARA, journals, contacts,
knowledge processing, agent support, and Dataview integrations.

A module may depend on documented core interfaces, but it must not depend on a
specific vault name or owner.

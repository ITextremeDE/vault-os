<!-- SPDX-License-Identifier: MPL-2.0 -->

Create a contact note from the input using the installed contact schema,
workflow, registers, and matching template. Include only supplied and verified
facts. Keep personal contact coordinates in the configured contact system when
one exists; add an external record identifier and URL only as a verified pair.
Do not repeat structured metadata as hard-coded prose. When a function in an
organization is supplied, create or update one role relation note from the Role
template instead of storing parallel organization and function lists in the
person note.

Input:

# Git commit plan skill

This optional skill prepares safe, coherent commits and can execute them only
with explicit authorization. Categories, prefixes, language, validators, and
secret exceptions are instance-owned policy.

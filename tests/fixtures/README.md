# Test vaults

Fixtures in this directory will verify clean installation, idempotent updates,
conflict detection, and preservation of instance-owned files.

Fixtures must contain synthetic data only.

# Vault-OS core

This directory will contain files that are portable, release-managed, and
required by every Vault-OS instance.

Core files must not contain source-vault names, personal data, organization
names, credentials, or absolute machine paths.
